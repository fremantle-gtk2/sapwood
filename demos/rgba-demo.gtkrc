# vim:set ft=gtkrc sw=2 sts=2 expandtab:
pixmap_path "images"

style "window" {
  engine "sapwood" {
    image {
      function = FLAT_BOX
      detail = "base"
      file = "window.png"
    }
  }
}

class "GtkWindow" style "window"

